apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starx-pro.fullname" . }}-test-connection"
  labels:
    {{- include "starx-pro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starx-pro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
