apiVersion: v1
kind: Service
metadata:
  name: {{ include "starx-pro.fullname" . }}
  labels:
    {{- include "starx-pro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 30002
  selector:
    {{- include "starx-pro.selectorLabels" . | nindent 4 }}
